class Node{
    constructor(value, parent, leftChild, rightChild){
        this.parent = parent;
        this.leftChild = leftChild;
        this.rightChild = rightChild;
    }
}

class Heap{
    constructor(arr){
        this.nodes = [];
    }

    function addNode(value){
        if(this.nodes = []){
            var parent = null;
            var leftChild = null;
            var rightChild = null;
            var newNode = new Node(value, parent, leftChild, rightChild);
        }else{
            
        }
    }
}